query AnimeList {
    Page{
        pageInfo{
            total
            currentPage
            lastPage
            hasNextPage
            perPage
        }
    }
    Media(type: ANIME) {
        title {
            romaji
            english
            native
        }
    }
}