package com.example.myanimection.models

/**Clase que determina las distintas categorías o listas a las que puede pertener un anime en la colección de un usuario.*/
enum class AnimeCategory {
        WATCHING,
        PENDING,
        DROPPED,
        COMPLETED
}